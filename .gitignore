.venv/
venv/
__pycache__/
*.py[cod]
*.so
.Python
*.egg-info/
.eggs/
dist/
build/

.env
.env.*
!.env.example

.DS_Store
.AppleDouble
Thumbs.db
.idea/
.vscode/
*.swp

# Type checkers / linters
.mypy_cache/
.ruff_cache/

# Cursor / local tooling (if present in repo root)
.cursor/

# TRIBE / HF / uv caches (large)
cache/
.huggingface/
hf_cache/

# Pipeline outputs and resume shards
outputs/
!outputs/photo_element_logreg.joblib
**/*_row_cache/

# Still-image → TRIBE: regenerated MP4s (large)
data/photo_dataset/generated_videos/

# Jupyter
.ipynb_checkpoints/

.pytest_cache/
.coverage
htmlcov/

# MediaPipe model (downloaded at runtime)
*.task

# Keys / secrets (never commit)
*.pem
id_rsa
id_ed25519

# Cloned repos (external dependencies)
gpype/

